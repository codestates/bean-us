import styled from 'styled-components';

export const TopFrame = styled.div`
  position: relative;
  width: 1200px;
  margin: auto;
  overflow: auto;

  & .title {
    padding: 1rem 0;
    font-size: 2rem;
    font-weight: bold;
  }
`;
